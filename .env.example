# Required: Overleaf Git token
OVERLEAF_TOKEN=your_overleaf_git_token

# Optional default project ID (you can also pass project_id per MCP tool call)
PROJECT_ID=your_overleaf_project_id

# Optional local directory where Overleaf repos are cloned/synced
OVERLEAF_REPO_DIR=/tmp/overleaf_repos
